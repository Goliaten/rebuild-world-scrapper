# Rebuild World scrapper
This script will scrape all chapters of Rebuild World from DivineDaoLibrary's site.

It's not fast, but it works.

All files will be saved to `chapters` directory, in `.md` format.